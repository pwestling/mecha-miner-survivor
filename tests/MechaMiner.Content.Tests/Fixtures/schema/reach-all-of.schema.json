{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Negative control: bare bound in allOf",
  "description": "A bare numeric bound with no adjacent x-authority, placed in an allOf branch. This file exists to prove the attribution walk reaches that position; it is never loaded as a real schema.",
  "allOf": [
    {
      "type": "integer"
    },
    {
      "type": "integer",
      "maximum": 2048
    }
  ]
}
