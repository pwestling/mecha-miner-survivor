{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mechaminer.local/schemas/boss.schema.json",
  "title": "SCH-CNT-002 interval boss definition",
  "description": "An interval boss definition. The project-owned typed validator in src/MechaMiner.Content/Categories/ remains authoritative; this schema is the draft 2020-12 mirror kept for editor and tool interoperability, and a fixture corpus proves the two reach the same accept/reject verdict on every structural case. See content/schemas/README.md. There is no armor field, on the same reading of doc 40's field list as the enemy schema.",
  "type": "object",
  "additionalProperties": false,
  "propertyNames": {
    "$ref": "#/$defs/snake_case_field"
  },
  "required": [
    "id",
    "schema_version",
    "content_version",
    "status",
    "tags",
    "source_refs",
    "arrival",
    "initial_hull",
    "contact_damage",
    "control_resistance_percent",
    "post_hard_control_immunity_seconds",
    "movement_speed_percent_of_mech_base",
    "contact_shape",
    "contact_and_weapon_hurt_diameter_m",
    "behavior_kind",
    "ability",
    "ability_timeline_cancellable_by_player_control",
    "persistence",
    "resonance",
    "defeat_reward",
    "description",
    "rules"
  ],
  "properties": {
    "id": {
      "title": "A stable, case-sensitive ASCII content ID",
      "type": "string",
      "pattern": "^BOSS-[0-9]{2}$"
    },
    "schema_version": {
      "$ref": "#/$defs/version"
    },
    "content_version": {
      "$ref": "#/$defs/version"
    },
    "status": {
      "$ref": "#/$defs/status"
    },
    "name_key": {
      "$ref": "#/$defs/name_key"
    },
    "summary_key": {
      "$ref": "#/$defs/summary_key"
    },
    "tags": {
      "$ref": "#/$defs/tags"
    },
    "source_refs": {
      "$ref": "#/$defs/source_refs"
    },
    "presentation_id": {
      "$ref": "#/$defs/presentation_id"
    },
    "arrival": {
      "title": "A boss's arrival",
      "description": "The warning lead is the schedule's, not the boss's, and lives on WAV-01.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "timecode": {
          "type": "string",
          "pattern": "^[0-9]{1,2}:[0-5][0-9]$",
          "description": "The run timecode at which the boss arrives. The seconds into the run are derived from it."
        }
      },
      "required": [
        "timecode"
      ]
    },
    "initial_hull": {
      "type": "integer",
      "description": "Hull integrity on arrival.",
      "exclusiveMinimum": 0,
      "x-authority": {
        "exclusiveMinimum": {
          "source": "TDD-CONTENT-DATA",
          "section": "Unit and numeric policy",
          "kind": "sourced",
          "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
        }
      }
    },
    "contact_damage": {
      "type": "integer",
      "description": "Damage one contact deals.",
      "exclusiveMinimum": 0,
      "x-authority": {
        "exclusiveMinimum": {
          "source": "TDD-CONTENT-DATA",
          "section": "Unit and numeric policy",
          "kind": "sourced",
          "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
        }
      }
    },
    "control_resistance_percent": {
      "type": "integer",
      "description": "The share of an applied control effect that is resisted.",
      "minimum": 0,
      "maximum": 100,
      "x-authority": {
        "minimum": {
          "source": "TDD-CONTENT-DATA",
          "section": "Unit and numeric policy",
          "kind": "sourced",
          "derivation": "the section scopes _percent to human-readable percentage points; the floor of a share of a whole is zero of them, which is the share that is none of it"
        },
        "maximum": {
          "source": "TDD-CONTENT-DATA",
          "section": "Unit and numeric policy",
          "kind": "sourced",
          "derivation": "the section scopes _percent to human-readable percentage points; the ceiling of a share of a whole is one hundred of them, which is the whole, and a larger value is not a share of anything"
        }
      }
    },
    "post_hard_control_immunity_seconds": {
      "type": "number",
      "description": "Immunity after a hard control effect resolves.",
      "minimum": 0,
      "x-authority": {
        "minimum": {
          "source": "TDD-CONTENT-DATA",
          "section": "Unit and numeric policy",
          "kind": "sourced",
          "derivation": "the section states that durations are nonnegative and that rates cannot be negative, so the floor of every duration and rate is zero and there is no upper bound the section supplies"
        }
      }
    },
    "movement_speed_percent_of_mech_base": {
      "type": "integer",
      "description": "Movement speed as a share of the mech's base speed.",
      "exclusiveMinimum": 0,
      "x-authority": {
        "exclusiveMinimum": {
          "source": "TDD-CONTENT-DATA",
          "section": "Unit and numeric policy",
          "kind": "sourced",
          "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
        }
      }
    },
    "contact_shape": {
      "type": "string",
      "description": "The footprint's shape.",
      "enum": [
        "circle"
      ]
    },
    "contact_and_weapon_hurt_diameter_m": {
      "type": "number",
      "description": "The authored contact circle. A boss authors its diameter directly because no accepted document gives a boss a body scale to derive it from; only the contact-begin centre distance is derived.",
      "exclusiveMinimum": 0,
      "x-authority": {
        "exclusiveMinimum": {
          "source": "TDD-CONTENT-DATA",
          "section": "Unit and numeric policy",
          "kind": "sourced",
          "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
        }
      }
    },
    "behavior_kind": {
      "type": "string",
      "description": "The registered behavior. A lower-kebab-case token. Whether it resolves to exactly one registered behavior descriptor is the behavior registry's check and is deliberately not asserted here.",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "ability": {
      "$ref": "#/$defs/ability"
    },
    "ability_timeline_cancellable_by_player_control": {
      "type": "boolean",
      "description": "Whether player control can cancel the ability timeline."
    },
    "persistence": {
      "title": "How a boss persists",
      "description": "Persistence rules.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "persists_until_killed": {
          "type": "boolean",
          "description": "Whether the boss persists until killed."
        },
        "never_despawns": {
          "type": "boolean",
          "description": "Whether the boss ever despawns."
        },
        "required_for_extraction": {
          "type": "boolean",
          "description": "Whether extraction requires the kill."
        },
        "counts_toward_ordinary_population": {
          "type": "boolean",
          "description": "Whether the boss counts against ordinary population ceilings."
        },
        "later_bosses_may_overlap": {
          "type": "boolean",
          "description": "Whether later bosses may overlap this one."
        },
        "maximum_simultaneous_bosses": {
          "type": "integer",
          "description": "How many bosses may be alive at once.",
          "minimum": 1,
          "x-authority": {
            "minimum": {
              "source": "TDD-CONTENT-DATA",
              "section": "Unit and numeric policy",
              "kind": "sourced",
              "derivation": "the section makes integer currency and rank values integral in source; a count of things that exist begins at one, because a count of zero is the absence of the field rather than a value of it"
            }
          }
        },
        "disposed_without_reward_if_run_extracts_while_alive": {
          "type": "boolean",
          "description": "Whether extracting while the boss lives forfeits its reward."
        },
        "spawn_rule": {
          "type": "string",
          "description": "Where the boss may enter."
        },
        "reentry": {
          "title": "How a boss re-enters",
          "description": "Re-entry rules.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "trigger": {
              "type": "string",
              "description": "What causes a re-entry."
            },
            "behavior": {
              "type": "string",
              "description": "How the boss re-enters."
            },
            "non_damaging_while_pending": {
              "type": "boolean",
              "description": "Whether the boss deals no damage while re-entry is pending."
            },
            "ability_cooldown_restarts_after_reentry": {
              "type": "boolean",
              "description": "Whether the ability cooldown restarts on re-entry."
            }
          },
          "required": [
            "trigger",
            "behavior",
            "non_damaging_while_pending",
            "ability_cooldown_restarts_after_reentry"
          ]
        }
      },
      "required": [
        "persists_until_killed",
        "never_despawns",
        "required_for_extraction",
        "counts_toward_ordinary_population",
        "later_bosses_may_overlap",
        "maximum_simultaneous_bosses",
        "disposed_without_reward_if_run_extracts_while_alive",
        "spawn_rule",
        "reentry"
      ]
    },
    "resonance": {
      "title": "How a boss interacts with resonance fields",
      "description": "Resonance interaction.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "receives_same_geode_resonance_modifiers_as_ordinary_enemies": {
          "type": "boolean",
          "description": "Whether geode resonance modifies the boss as it does an ordinary enemy."
        },
        "control_resistance_combines_with_driftmetal": {
          "type": "string",
          "description": "How the boss's resistance combines with the Driftmetal field."
        }
      },
      "required": [
        "receives_same_geode_resonance_modifiers_as_ordinary_enemies",
        "control_resistance_combines_with_driftmetal"
      ]
    },
    "defeat_reward": {
      "title": "A boss's defeat reward",
      "description": "The boss is the single writer on these amounts; the resource definitions used to carry copies of them in an economy rollup.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "common_ore": {
          "type": "integer",
          "description": "Common ore awarded.",
          "minimum": 0,
          "x-authority": {
            "minimum": {
              "source": "TDD-CONTENT-DATA",
              "section": "Unit and numeric policy",
              "kind": "sourced",
              "derivation": "the section makes counts integral; this one is summed into a report column across the catalog, so zero is authored rather than omitted and the floor is zero rather than one"
            }
          }
        },
        "specialized_material_units": {
          "type": "integer",
          "description": "Specialized material units awarded.",
          "minimum": 0,
          "x-authority": {
            "minimum": {
              "source": "TDD-CONTENT-DATA",
              "section": "Unit and numeric policy",
              "kind": "sourced",
              "derivation": "the section makes counts integral; this one is summed into a report column across the catalog, so zero is authored rather than omitted and the floor is zero rather than one"
            }
          }
        },
        "unsecured_hyper_gold": {
          "type": "integer",
          "description": "Unsecured Hyper Gold awarded.",
          "minimum": 0,
          "x-authority": {
            "minimum": {
              "source": "TDD-CONTENT-DATA",
              "section": "Unit and numeric policy",
              "kind": "sourced",
              "derivation": "the section makes counts integral; this one is summed into a report column across the catalog, so zero is authored rather than omitted and the floor is zero rather than one"
            }
          }
        },
        "material_selection": {
          "type": "string",
          "description": "How the awarded materials are chosen."
        },
        "delivery": {
          "type": "string",
          "description": "How the reward is delivered."
        },
        "pauses_timer_or_opens_ui": {
          "type": "boolean",
          "description": "Whether collecting the reward pauses the run or opens a screen."
        }
      },
      "required": [
        "common_ore",
        "specialized_material_units",
        "unsecured_hyper_gold",
        "material_selection",
        "delivery",
        "pauses_timer_or_opens_ui"
      ]
    },
    "description": {
      "type": "string",
      "description": "The authored roster description."
    },
    "rules": {
      "type": "array",
      "description": "Rule clarifications with no structured twin.",
      "items": {
        "type": "string"
      }
    }
  },
  "$defs": {
    "snake_case_field": {
      "title": "A schema-declared field name",
      "description": "Property names use snake_case.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$"
    },
    "version": {
      "title": "A positive integer version",
      "description": "The initial schema_version and content_version of every first-authored definition is 1.",
      "type": "integer",
      "minimum": 1,
      "x-authority": {
        "minimum": {
          "source": "TDD-CONTENT-DATA",
          "section": "Initial versions",
          "kind": "sourced",
          "derivation": "the initial value of every first-authored definition is 1 and both versions only ever increment, so no valid version is below 1; there is no upper bound because a definition may be revised without limit"
        }
      }
    },
    "status": {
      "title": "Lifecycle state",
      "type": "string",
      "enum": [
        "development",
        "enabled",
        "disabled",
        "retired"
      ]
    },
    "name_key": {
      "title": "Localization key for the player-facing name",
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*\\.[A-Za-z0-9][A-Za-z0-9_-]*\\.name$"
    },
    "summary_key": {
      "title": "Localization key for the concise player-facing summary",
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*\\.[A-Za-z0-9][A-Za-z0-9_-]*\\.summary$"
    },
    "tags": {
      "title": "Closed query and tooling vocabulary",
      "description": "The vocabulary starts empty, so [] is the only accepted value.",
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": []
      }
    },
    "source_refs": {
      "title": "The sources this definition implements",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^(?:[a-z][a-z0-9_]*(?:\\.[a-z][a-z0-9_]*|\\[\\]|\\[[0-9]+\\]|\\[[0-9]+\\.\\.[0-9]+\\])*: )?(?:(?:GDD|TDD)-[A-Z0-9-]+|DEC-[0-9]{3}|TDR-[0-9]{3}|TR-[A-Z]+-[0-9]{3})(?:#[a-z0-9-]+)?$"
      }
    },
    "presentation_id": {
      "title": "Logical presentation entry",
      "description": "Omitted when a definition never appears in-world. No accepted document mints a grammar for a presentation ID, and inventing one for a category with zero members would be structure ahead of need, so the only constraint is that an authored value is not the empty string.",
      "type": "string",
      "minLength": 1,
      "x-authority": {
        "minLength": {
          "kind": "structural",
          "rationale": "not a capacity bound: the empty string is what the compiler materializes for an omitted field, so an authored empty string would be a second way to say 'absent'. 1 is the smallest length that excludes it"
        }
      }
    },
    "ability": {
      "title": "A boss ability",
      "description": "A four-arm discriminated union on kind. The typed validator reaches the same verdict by selecting the arm from the parsed kind and reporting a foreign parameter as an arm mismatch; the two report it with different codes, so the agreement gate compares verdicts and not codes.",
      "oneOf": [
        {
          "title": "The straight-charge ability arm",
          "description": "A parameter of another arm is rejected here rather than ignored, because a charge duration on a leap would describe a timeline the ability does not have.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "kind": {
              "type": "string",
              "const": "straight-charge"
            },
            "cadence_seconds": {
              "type": "number",
              "description": "The interval between ability activations.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "defines_own_damage_event": {
              "type": "boolean",
              "description": "Whether the ability's damage is its own event rather than ordinary contact."
            },
            "rules": {
              "type": "array",
              "description": "Rule clarifications with no structured twin.",
              "items": {
                "type": "string"
              }
            },
            "charge_contact_damage": {
              "type": "integer",
              "description": "Damage the charge deals.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "charge_duration_seconds": {
              "type": "number",
              "description": "How long the charge lasts.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "stop_and_telegraph_duration_seconds": {
              "type": "number",
              "description": "How long the boss telegraphs before charging.",
              "minimum": 0,
              "x-authority": {
                "minimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section states that durations are nonnegative and that rates cannot be negative, so the floor of every duration and rate is zero and there is no upper bound the section supplies"
                }
              }
            },
            "ordinary_contact_damage_replaced_during_charge": {
              "type": "integer",
              "description": "The ordinary contact damage the charge replaces.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "charge_speed_percent_of_mech_base": {
              "type": "integer",
              "description": "Charge speed as a share of the mech's base speed.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "turns_during_charge": {
              "type": "boolean",
              "description": "Whether the boss can turn mid-charge."
            }
          },
          "required": [
            "kind",
            "cadence_seconds",
            "defines_own_damage_event",
            "rules"
          ]
        },
        {
          "title": "The incomplete-minion-ring ability arm",
          "description": "A parameter of another arm is rejected here rather than ignored, because a charge duration on a leap would describe a timeline the ability does not have.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "kind": {
              "type": "string",
              "const": "incomplete-minion-ring"
            },
            "cadence_seconds": {
              "type": "number",
              "description": "The interval between ability activations.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "defines_own_damage_event": {
              "type": "boolean",
              "description": "Whether the ability's damage is its own event rather than ordinary contact."
            },
            "rules": {
              "type": "array",
              "description": "Rule clarifications with no structured twin.",
              "items": {
                "type": "string"
              }
            },
            "pause_duration_seconds": {
              "type": "number",
              "description": "How long the boss pauses to spawn.",
              "minimum": 0,
              "x-authority": {
                "minimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section states that durations are nonnegative and that rates cannot be negative, so the floor of every duration and rate is zero and there is no upper bound the section supplies"
                }
              }
            },
            "ring_opening_degrees": {
              "type": "integer",
              "description": "The angular gap left in the ring.",
              "minimum": 0,
              "maximum": 360,
              "x-authority": {
                "minimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section names _degrees as a unit suffix; the smallest gap in a ring is no gap at all, which is zero degrees"
                },
                "maximum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section names _degrees as a unit suffix; the largest gap in a ring is the whole circle, which is three hundred and sixty degrees"
                }
              }
            },
            "spawn_count": {
              "type": "integer",
              "description": "How many minions the ring places.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "spawn_enemy_id": {
              "type": "string",
              "pattern": "^EN-[0-9]{2}$",
              "description": "The enemy the ring spawns."
            },
            "spawn_drops_loot": {
              "type": "boolean",
              "description": "Whether ring minions drop loot."
            },
            "spawn_remains_linked_to_boss": {
              "type": "boolean",
              "description": "Whether ring minions stay linked to the boss after creation."
            }
          },
          "required": [
            "kind",
            "cadence_seconds",
            "defines_own_damage_event",
            "rules"
          ]
        },
        {
          "title": "The radial-projectile-burst ability arm",
          "description": "A parameter of another arm is rejected here rather than ignored, because a charge duration on a leap would describe a timeline the ability does not have.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "kind": {
              "type": "string",
              "const": "radial-projectile-burst"
            },
            "cadence_seconds": {
              "type": "number",
              "description": "The interval between ability activations.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "defines_own_damage_event": {
              "type": "boolean",
              "description": "Whether the ability's damage is its own event rather than ordinary contact."
            },
            "rules": {
              "type": "array",
              "description": "Rule clarifications with no structured twin.",
              "items": {
                "type": "string"
              }
            },
            "stop_and_charge_duration_seconds": {
              "type": "number",
              "description": "How long the boss charges before the burst.",
              "minimum": 0,
              "x-authority": {
                "minimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section states that durations are nonnegative and that rates cannot be negative, so the floor of every duration and rate is zero and there is no upper bound the section supplies"
                }
              }
            },
            "projectile_count": {
              "type": "integer",
              "description": "How many projectiles the burst fires.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "radial_offset_alternation_degrees": {
              "type": "integer",
              "description": "How far successive bursts rotate.",
              "minimum": 0,
              "maximum": 360,
              "x-authority": {
                "minimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section names _degrees as a unit suffix; the smallest rotation offset is no rotation, which is zero degrees"
                },
                "maximum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section names _degrees as a unit suffix; the largest rotation offset before the offsets repeat is a full turn, which is three hundred and sixty degrees"
                }
              }
            },
            "projectile": {
              "$ref": "#/$defs/projectile"
            }
          },
          "required": [
            "kind",
            "cadence_seconds",
            "defines_own_damage_event",
            "rules"
          ]
        },
        {
          "title": "The locked-marker-leap ability arm",
          "description": "A parameter of another arm is rejected here rather than ignored, because a charge duration on a leap would describe a timeline the ability does not have.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "kind": {
              "type": "string",
              "const": "locked-marker-leap"
            },
            "cadence_seconds": {
              "type": "number",
              "description": "The interval between ability activations.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "defines_own_damage_event": {
              "type": "boolean",
              "description": "Whether the ability's damage is its own event rather than ordinary contact."
            },
            "rules": {
              "type": "array",
              "description": "Rule clarifications with no structured twin.",
              "items": {
                "type": "string"
              }
            },
            "crouch_duration_seconds": {
              "type": "number",
              "description": "How long the boss crouches before leaping.",
              "minimum": 0,
              "x-authority": {
                "minimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section states that durations are nonnegative and that rates cannot be negative, so the floor of every duration and rate is zero and there is no upper bound the section supplies"
                }
              }
            },
            "landing_damage": {
              "type": "integer",
              "description": "Damage the landing deals.",
              "exclusiveMinimum": 0,
              "x-authority": {
                "exclusiveMinimum": {
                  "source": "TDD-CONTENT-DATA",
                  "section": "Unit and numeric policy",
                  "kind": "sourced",
                  "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
                }
              }
            },
            "marker_shape": {
              "type": "string",
              "description": "The footprint's shape.",
              "enum": [
                "circle"
              ]
            },
            "marker_tracks_after_appearing": {
              "type": "boolean",
              "description": "Whether the marker follows the mech after it appears."
            },
            "airborne_deals_contact_damage": {
              "type": "boolean",
              "description": "Whether the airborne boss deals contact damage."
            },
            "airborne_remains_targetable": {
              "type": "boolean",
              "description": "Whether the airborne boss can be hit."
            },
            "resumes_pursuit_immediately_after_landing": {
              "type": "boolean",
              "description": "Whether pursuit resumes the moment the boss lands."
            }
          },
          "required": [
            "kind",
            "cadence_seconds",
            "defines_own_damage_event",
            "rules"
          ]
        }
      ]
    },
    "projectile": {
      "title": "A projectile",
      "description": "Shared by an enemy's specialist attack and a boss's radial burst, because doc 40 names projectile and boss-ability parameters as one field group. There is no lifetime_seconds: both projectiles state their lifetime only as prose, and authoring a number no document states would be inventing a balance value.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "damage": {
          "type": "integer",
          "description": "Damage one projectile deals.",
          "exclusiveMinimum": 0,
          "x-authority": {
            "exclusiveMinimum": {
              "source": "TDD-CONTENT-DATA",
              "section": "Unit and numeric policy",
              "kind": "sourced",
              "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
            }
          }
        },
        "speed_percent_of_mech_base": {
          "type": "integer",
          "description": "Speed as a share of the mech's base speed. The world speed is derived from this and the player baseline, and is never authored.",
          "exclusiveMinimum": 0,
          "x-authority": {
            "exclusiveMinimum": {
              "source": "TDD-CONTENT-DATA",
              "section": "Unit and numeric policy",
              "kind": "sourced",
              "derivation": "the section makes durations nonnegative and rates non-negative; this quantity is additionally one the definition divides by, scales by, or spends, so zero is excluded as well and the bound is exclusive rather than inclusive"
            }
          }
        },
        "homing": {
          "type": "boolean",
          "description": "Whether the projectile tracks its target."
        },
        "leads_target": {
          "type": "boolean",
          "description": "Whether the projectile aims ahead of a moving target."
        },
        "retargets": {
          "type": "boolean",
          "description": "Whether the projectile can change target after creation."
        },
        "splits": {
          "type": "boolean",
          "description": "Whether the projectile splits."
        },
        "explodes": {
          "type": "boolean",
          "description": "Whether the projectile explodes."
        },
        "leaves_hazard": {
          "type": "boolean",
          "description": "Whether the projectile leaves a lasting hazard."
        },
        "applies_status": {
          "type": "boolean",
          "description": "Whether the projectile applies a status."
        },
        "snapshot_at_creation": {
          "type": "array",
          "description": "The properties sampled when the projectile is created rather than read live.",
          "items": {
            "type": "string",
            "description": null,
            "enum": [
              "speed",
              "damage",
              "lifetime",
              "terrain-collision",
              "no-homing"
            ]
          },
          "uniqueItems": true
        }
      },
      "required": [
        "damage",
        "speed_percent_of_mech_base",
        "homing",
        "leads_target",
        "retargets",
        "splits",
        "explodes",
        "leaves_hazard",
        "applies_status",
        "snapshot_at_creation"
      ]
    }
  }
}
