{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Negative control: bare bound in anyOf",
  "description": "A bare numeric bound with no adjacent x-authority, placed in an anyOf branch. This file exists to prove the attribution walk reaches that position; it is never loaded as a real schema.",
  "anyOf": [
    {
      "type": "integer"
    },
    {
      "type": "integer",
      "maximum": 2048
    }
  ]
}
