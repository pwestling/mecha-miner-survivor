{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Negative control: bare bound in contains",
  "description": "A bare numeric bound with no adjacent x-authority, placed in a contains subschema, a keyword this evaluator does not implement. This file exists to prove the attribution walk reaches that position; it is never loaded as a real schema.",
  "type": "array",
  "contains": {
    "type": "integer",
    "maximum": 2048
  }
}
