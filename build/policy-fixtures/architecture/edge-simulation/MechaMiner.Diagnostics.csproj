<Project Sdk="Microsoft.NET.Sdk">

  <!--
    NEGATIVE control. Deliberately violates doc 115 § Accepted project boundary: the
    MechaMiner.Diagnostics row allows ".NET base libraries only", and this edge would
    additionally make the diagnostics leaf depend on the run domain, which doc 115
    § Accepted project boundary forbids in that direction ("Simulation deliberately does
    not reference it"). See ../README.md. Not in MechaMiner.sln.
  -->

  <ItemGroup>
    <ProjectReference Include="../../../../src/MechaMiner.Simulation/MechaMiner.Simulation.csproj" />
  </ItemGroup>

</Project>
