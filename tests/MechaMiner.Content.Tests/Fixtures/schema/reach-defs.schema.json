{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Negative control: bare bound in $defs",
  "description": "A bare numeric bound with no adjacent x-authority, placed in a root $defs entry. This file exists to prove the attribution walk reaches that position; it is never loaded as a real schema.",
  "type": "object",
  "$defs": {
    "capacity": {
      "type": "integer",
      "maximum": 2048
    }
  }
}
