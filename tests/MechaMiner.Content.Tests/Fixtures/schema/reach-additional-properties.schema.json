{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Negative control: bare bound in additionalProperties",
  "description": "A bare numeric bound with no adjacent x-authority, placed in an additionalProperties subschema object. This file exists to prove the attribution walk reaches that position; it is never loaded as a real schema.",
  "type": "object",
  "additionalProperties": {
    "type": "integer",
    "maximum": 2048
  }
}
